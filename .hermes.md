## Règles pour ce projet

### Auto-freelance Pipeline
- Config dans `config.yml` — profil Dan Saffar, 700€ TJM, compétences Claude/IA/formation.
- Toujours lancer avec `--submit` pour la soumission automatique.
- `pipeline.db` = SQLite local, ne pas commit.
- Tests : 56/56, lancer `pytest tests/ -q` avant tout changement.
- Sessions dans `/data/agents/hermes/.auto-freelance-sessions/`.
- Les mappers de soumission sont dans `job_scanner/submission/forms/`.

### Règles
- `config.yml` contient des données personnelles → jamais commit.
- Ne pas modifier `CLAUDE.md` sans revue.
- Le fallback `copy_text` Telegram est limité à 256 caractères.